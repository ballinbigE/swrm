public enum WhatsNew {
    public static let version = "0.3.0"
    public static let title = "swrm v0.3.0 — Projects on the Web"
    public static let items: [String] = [
        "🗂️ Projects on the web. The localhost board juggles multiple repos now — register a project, switch from the dropdown, or keep two tabs open side by side.",
        "🔭 Everything scopes per project. Boards, tasks, markdown sync, and skill runs all stay in their own lane — no cross-project bleed.",
        "🍯 One swrm, every surface. Project switching now lives on Mac, iOS, and the web."
    ]
}
